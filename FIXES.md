# رفيق القرآن — تقرير الإصلاح

تاريخ المراجعة: 2026-09-17
النسخة المرجعية: `Rafiq-Quran-report-fix.zip`

كل تعديل هنا نابع من قراءة الكود لا من التخمين. البيئة المستخدمة في المراجعة
لا يتوفر فيها متصفح (نفس القيد المذكور في `QA-NOTES.md`)، لذلك الفحص ستاتيكي
بالكامل + اختبار تحميل في بيئة DOM مُصطنعة. **اختبار النقر التفاعلي النهائي
ما زال مطلوبًا على جهازك.**

---

## لماذا لم يمسك `preflight` القديم هذه الأخطاء؟

الأداة القديمة كانت تفحص **الشكل**: تكرار المعرّفات، توازن أقواس CSS، صحة
JSON، و`node --check` للصيغة. كل الأخطاء أدناه تعدّي من هذه الفحوص لأنها
أخطاء **سلوك** لا صيغة. تم توسيع `tools/preflight.mjs` ليغطيها (البند 14).

---

## أولًا: أخطاء توقف التنفيذ

### 1 — `ReferenceError` في الحديث اليومي
**الملف:** `js/app.js` · دالة `fetchOnlineHadith()`

المتغير `number` كان مستخدمًا داخل قوالب نصية دون أي تعريف. بناء مصفوفة
`urls` يحدث **قبل** كتلة `try`، فالدالة كانت ترمي استثناءً فور استدعائها
وينهار مسار الحديث اليومي الأونلاين بالكامل. الدالة المجاورة
`fetchOnlineAthar()` تعرّف `const number` بشكل سليم — يبدو أن السطر حُذف سهوًا.

```js
const number=hadithNumberForDay();   // مضاف
```

استُخدمت `hadithNumberForDay()` الموجودة أصلًا لأنها ثابتة لكل يوم، وهو السلوك
المقصود للمحتوى اليومي (بعكس العشوائي في `fetchOnlineAthar`).

### 2 — `ReferenceError` عند تغيير حجم الشاشة
**الملف:** `js/app.js` · آخر سطر

مستمع `resize` كان موضوعًا **بعد** `})();` أي في النطاق العام، بينما
`renderProgressDashboard()` معرّفة **داخل** الـ IIFE. أي تغيير لحجم النافذة
وأنت في صفحة التقدم كان يرمي استثناءً.

تم التحقق من الخطأ بإعادة إنتاجه معزولًا:
`ReferenceError: renderProgressDashboard is not defined`

**الإصلاح:** نقل المستمع إلى داخل الـ IIFE قبل سطر الإغلاق.

### 3 — دالة تُستدعى ولا وجود لها
**الملف:** `js/app.js` · `stopRecitation()`

`window.syncRafiqPauseButton?.()` لم تكن معرّفة في أي ملف من ملفات المشروع.
لم تكن ترمي خطأ بفضل `?.` لكن زر الإيقاف لم يكن يتزامن أبدًا.

**الإصلاح:** تعريف الدالة فعليًا وتصديرها:

```js
function syncPauseButton(){const t=$('#playerToggle'); if(t)t.textContent=qAudio.paused?'استمرار':'إيقاف مؤقت';}
window.syncRafiqPauseButton=syncPauseButton;
```

---

## ثانيًا: أخطاء منطقية صامتة

### 4 — المحتوى اليومي الأونلاين كان يُقفل طوال اليوم
**الملف:** `js/app.js` · `refreshDailyOnline()`

`renderDailyHome()` تحفظ نسخة `local-fallback` في `state.dailyContent`، ثم
`refreshDailyOnline(false)` كانت ترجع مبكرًا عند أي `cache?.source` — و
`'local-fallback'` قيمة صادقة. النتيجة: أول رندر يقفل اليوم كله على المحتوى
المحلي ولا يحاول الاتصال مرة أخرى.

```js
// قبل:  if(!force&&cache?.key===key&&cache?.source) return cache;
// بعد:  if(!force&&cache?.key===key&&cache?.source==='online') return cache;
```

### 5 — قائمة المحفوظ المسجّل لم تكن تظهر إطلاقًا
**الملف:** `js/memorization-engine.js` · `renderSaved()` و `injectUI()`

`renderSaved()` تبحث عن `#memSavedList`، والحاوية لم تكن موجودة لا في
`index.html` ولا ضمن قالب `injectUI()`، رغم وجود كل شقيقاتها
(`memWeekGrid`, `memUpcomingList`, `memWeakList`, `memGapsList`,
`memHistoryList`). الدالة كانت ترجع بصمت وتُسقط الميزة كلها.

**الإصلاح:** إضافة القسم داخل «تفاصيل المتابعة»:

```html
<details class="mem-collapse"><summary>المحفوظ المسجّل</summary>
  <div class="mem-collapse-body"><div class="mem-range-list" id="memSavedList"></div></div>
</details>
```

### 6 — قراءة نظام المراجعة من معرّف غير موجود
**الملف:** `js/memorization-engine.js` · مسار حفظ الخطة

`$('#planReviewMode')?.value` كانت دائمًا `undefined`، لأن أزرار الراديو تحمل
`name="planReviewMode"` وليس `id`. القيمة كانت تسقط دائمًا على
`data.plan.reviewMode` القديمة. لاحظ أن `syncReviewMode` في نفس الملف تستخدم
المُحدِّد الصحيح — فالتناقض داخلي.

```js
document.querySelector('input[name="planReviewMode"]:checked')?.value || data.plan.reviewMode
```

### 7 — مؤشر التلاوات كان مزيفًا
**الملف:** `js/settings.js`

`mark('التلاوات', false)` كانت قيمة ثابتة مكتوبة يدويًا، فالمؤشر يقول
«غير جاهزة» حتى لو كانت التلاوات محفوظة فعلًا.

**الإصلاح:** قراءة الحالة الحقيقية من كاش الصوت وعرض العدد:

```js
async function savedRecitations(){try{const c=await caches.open('rafiq-audio-v1');return (await c.keys()).length}catch{return 0}}
```

---

## ثالثًا: تنظيف

### 8 — بريد placeholder في الإنتاج
**الملف:** `js/app.js` · زر «بلّغ عن مشكلة»

`const email='YOUR_EMAIL_HERE'` — الزر كان ينسخ النص حرفيًا ويفتح
`mailto:YOUR_EMAIL_HERE`.

**الإصلاح:** ثابت واضح في أعلى الملف:

```js
// ضع بريدك هنا لتفعيل زر «بلّغ عن مشكلة». إذا تُرك فارغًا يُخفى الزر تلقائيًا.
const RAFIQ_CONTACT_EMAIL='';
```

> **مطلوب منك:** ضع بريدك في هذا الثابت. طالما هو فارغ سيبقى الزر مخفيًا —
> وهذا أفضل من زر يعمل بشكل خاطئ.

### 9 — كود ميت
- `maybeOpenDailyWelcome` كانت معرّفة **مرتين** بنفس الجسم؛ الثانية تلغي الأولى → حُذفت.
- `focusWelcomeName()` معرّفة ولا تُستدعى من أي مكان، وتشير إلى `#welcomeName`
  و`#closeWelcomeBtn` من شاشة ترحيب محذوفة (`renderWelcome(){}` صارت فارغة) → حُذفت.
- `if(typeof updatePageAmbient==='function') updatePageAmbient(view);` — لا يوجد
  ملف في المشروع يعرّف `updatePageAmbient`، فالشرط لا يتحقق أبدًا → حُذف السطر.
  *(لو كنت تنوي إضافتها لاحقًا، أعد السطر بعد تعريف الدالة وتصديرها على `window`.)*
- `$('#todayFocusContent')||$('#todayList')` — الشق الثاني لا يقابل أي عنصر → حُذف.

### 10 — مسافات غير مرئية داخل الكود
**الملف:** `js/app.js`

**199 حرف NBSP** (`U+00A0`) كانت مستخدمة كمسافات بادئة، منها 155 في بداية
السطور. هذا الحرف مسافة صالحة في JavaScript فلا يسبب خطأ صيغة، لكنه يكسر
كثيرًا من أدوات البناء والـ linters وهو غير مرئي تمامًا أثناء المراجعة.

تم تحويلها إلى مسافات عادية **مع الحرص على عدم لمس أي NBSP داخل النصوص
العربية** (الاستبدال مقصور على المسافات البادئة في بداية السطر).

### 11 — ترتيب العناصر والسكربتات
**الملف:** `index.html`

`methodModal`, `methodList`, `privacyModal` كانت موضوعة **بعد** وسوم
`<script>`. السكربتات تُنفَّذ أثناء تحليل الصفحة، فاستدعاء `renderMethod()` عند
الإقلاع كان يجد `null` ويرجع فارغًا.

**الإصلاح:** نقل كل العناصر قبل السكربتات، والسكربتات كلها قبل `</body>`.

---

## رابعًا: الأداء

### 12 — إعادة كتابة `sw.js`
**قبل:** استراتيجية network-first على **كل شيء** مع `{cache:'no-store'}`. أي أن
كل فتحة للتطبيق كانت تعيد تنزيل ~2.5 ميجابايت (منها `quran-uthmani.json`
بحجم 1.5 ميجا) حتى وهي مخزّنة بالفعل. هذا كان يُلغي عمليًا نية
`{cache:'force-cache'}` المكتوبة في `app.js`.

**كذلك:** قائمة `CORE` تخزّن `./css/app.css` بينما الصفحة تطلب
`./css/app.css?v=perf-ui-fixes-1` — أي أن الـ precache لم يكن يُطابَق مطابقة
دقيقة أصلًا.

**بعد:**
- تنقّلات HTML: network-first مع رجوع إلى `index.html` عند انقطاع الاتصال.
- الأصول الثابتة (css/js/json/صور): **cache-first** مع تحديث في الخلفية.
- مطابقة دقيقة أولًا ثم مطابقة متجاهلة لـ `?v=` — حتى يعمل تحديث الإصدار
  بشكل صحيح ولا تُقدَّم نسخة قديمة عند رفع رقم `?v=`.
- رفع اسم الكاش إلى `rafiq-offline-v39` ليُفعّل مسار التنظيف في `activate`.

---

## خامسًا: تحسينات مضافة

### 13 — حل الأجزاء بدون إنترنت
**الملف:** `js/quran-index.js`

`resolveJuz` كانت تعتمد على فهرس بعيد أو على `api.alquran.cloud`. النتيجة:
خطة حفظ بوحدة «جزء» تفشل بلا اتصال — رغم أن المصحف كاملًا مرفق محليًا.

**الإصلاح:** جدول بدايات الأجزاء الثلاثين (رواية حفص) مضمّن في الملف، وحدود
النهاية تُحسب من عدد آيات السور المقروء من `quran-uthmani.json` أو من
`window.RAFIQ_QURAN`.

**التحقق:** الأجزاء الثلاثون تغطي **6236 آية بالضبط**، متتالية، بلا فجوة ولا
تداخل — مطابقةً للمصحف المرفق. وتم التأكد من مطابقة مطالع الأجزاء
(«سيقول السفهاء»، «تلك الرسل»، «سبحان الذي أسرى»، «عم يتساءلون» …).

الجدول يُستشار **أولًا**، فتصبح الاستجابة فورية حتى مع وجود اتصال.

> ملاحظة: **الصفحات** (`page`) ما زالت تحتاج مصدرًا خارجيًا، لأن
> `quran-uthmani.json` لا يحتوي على أرقام الصفحات. لو أردت الصفحات أوفلاين
> أيضًا فالحل هو إضافة جدول ترقيم صفحات إلى الحزمة.

### 14 — بوابة `preflight` أقوى
**الملف:** `tools/preflight.mjs`

فحوص جديدة تمسك **بالضبط** أصناف الأخطاء أعلاه:

| الفحص | يمسك |
|---|---|
| كود بعد `})();` | الخطأ #2 |
| معرّفات غير معرّفة (AST) | الخطأ #1 |
| دوال مكررة في نفس النطاق | الخطأ #9 |
| `#id` لا يقابل عنصرًا في HTML ولا في قوالب JS | الأخطاء #5 و #6 |
| نصوص placeholder | الخطأ #8 |
| NBSP كمسافات بادئة | الخطأ #10 |
| markup بعد وسوم `<script>` | الخطأ #11 |
| ملفات `sw.js` المفقودة من القرص | وقاية |

فحص «المعرّفات غير المعرّفة» يحتاج `acorn`. بدونه تتخطاه الأداة برسالة واضحة
بدل أن تفشل:

```bash
npm i -D acorn
node tools/preflight.mjs
```

### 15 — اختبار تحميل بلا متصفح
**الملف الجديد:** `tools/smoke.mjs`

ينفّذ كل السكربتات بترتيب `index.html` داخل DOM مُصطنع ويكشف أي استثناء وقت
التحميل، ويتحقق من وجود الكائنات العامة المتوقعة.

```bash
node tools/smoke.mjs
```

---

## النتيجة

```
$ node tools/preflight.mjs
All preflight checks passed.

$ node tools/smoke.mjs
Smoke test clean: every module loaded without throwing.
```

النسخة الأصلية على نفس الاختبارين: **3 إخفاقات** في الـ smoke، و**فحصان
فاشلان** في الـ preflight الجديد.

---

## ما لم أغيّره عمدًا

### الأيقونات
`assets/icon.svg` حجمه **615 كيلوبايت** وهو في الحقيقة صورة PNG مغلّفة داخل
SVG — مبالغة كبيرة لأيقونة تُعرض بحجم 64 بكسل أو أقل. و`icon-512.png` حجمه
**461 كيلو**.

جرّبت الضغط. الضغط الذي يوفّر ~65% **ليس بلا فقد**: يغيّر البكسلات فعليًا
(فرق يصل إلى 44 في بعض القنوات). أما الضغط بلا فقد فعليًا فينتج ملفًا **أكبر**
من الأصل (83 كيلو مقابل 71) — أي أن ملفاتك مضغوطة جيدًا بالفعل.

لذلك **أعدت الأيقونات الثلاث مطابقة بايت-ببايت للأصل**. تغيير تصميمك قرارك
أنت لا قراري. لو أردت التوفير، أوضح مسار هو تصغير الصورة المضمّنة في
`icon.svg` من 512 إلى 256 بكسل (615 كيلو ← ~60 كيلو) — لن يُلاحظ الفرق لأن
الأيقونة لا تُعرض أصلًا بأكثر من 64 بكسل.

### أمور رصدتها ولم تكن أخطاء
- **الهروب من HTML سليم.** كل المحتوى القادم من الشبكة (التفسير، معاني
  الكلمات، أسباب النزول، الحديث) يمرّ عبر `esc()` أو `textContent`. لا توجد
  ثغرة XSS في المسارات التي فحصتها.
- `window.__rafiqSuppressLastJump` تُقرأ ولا تُكتب في أي مكان — أثرها أن
  القفز لآخر موضع يحدث دائمًا. تركتها لأنها على الأرجح السلوك المقصود.
- `focusModeBtn` و`focusExitBtn` يحملان `hidden` في HTML — معطّلان عمدًا.

---

## ما زال مطلوبًا منك

1. ضع بريدك في `RAFIQ_CONTACT_EMAIL` داخل `js/app.js`.
2. **اختبار نقر كامل على Chrome/Edge** — لا بديل عنه، والبيئة هنا بلا متصفح.
   ركّز على: الحديث اليومي بعد منتصف الليل، تغيير حجم النافذة في صفحة التقدم،
   قائمة «المحفوظ المسجّل»، حفظ نظام المراجعة، والعمل بلا اتصال بعد فتحة أولى.
3. بعد نشر التحديث، تأكد أن عامل الخدمة القديم استُبدل (رقم الكاش تغيّر إلى
   `v39` فالتنظيف يحدث تلقائيًا، لكن قد تحتاج إغلاق كل التبويبات مرة واحدة).
